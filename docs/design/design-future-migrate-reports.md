# FUTURE — Migrate Reports to FastAPI + React

Status: NOT STARTED (future task)
Date: 2026-07-27
Repo: Pocket-Smith-Reports

## Summary
Migrate existing report pipelines (v4, mom, mega) to FastAPI endpoints + React UI.
Migrate as we go while building F1-F7.

## Scope
- v4 (monthly) → FastAPI endpoint + React page
- mom (multi-month) → FastAPI endpoint + React page
- mega (configurable period) → FastAPI endpoint + React page
- Existing CLI scripts remain as-is (backward compat)

## L1-L5: TBD (separate task)
// MegaBarChart — reuses F1.2 HorizontalBarChart (compatible props: data, title, width, barHeight, gap).
// No new component needed — HorizontalBarChart handles horizontal bar rendering.

export { HorizontalBarChart as MegaBarChart } from "@/components/reports/charts/HorizontalBarChart";